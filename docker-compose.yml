services:
  job-agent:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    image: job-agent:latest
    container_name: job-agent
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    network_mode: "bridge"